# AGENTS.md

このリポジトリでは、映画鑑賞記録アプリ `cinelog-calendar` のMVPを段階的に育てていきます。  
実装や提案を行うときは、以下の前提と判断基準に揃えてください。

## 作業原則

- MVPを優先する
- UIは日本語のみを前提にする
- まずはローカル保存で成立させる
- 将来拡張を見越しつつ、初期実装はシンプルに保つ

## プロダクト前提

- ログイン機能は入れない
- 単一端末での利用を前提にする
- 映画記録は1日に複数件登録できる
- 評価項目はMVPでは固定5項目とする
- ランキングタブはMVPに含める

## UX方針

- アプリ体験の中心はカレンダーとする
- 日付を起点に、その日の記録一覧と新規登録へ進める流れを基本とする
- 登録編集はモーダルルートベースのハイブリッド設計にする
- iOSでは `formSheet` を優先し、親画面の文脈を保ちながら入力できる形を目指す
- Androidでは入力のしやすさを優先し、全画面寄りの体験を許容する

## データと実装の不変条件

- 各評価は `0.0` から `5.0` までの `0.5` 刻み
- 総合点 `totalScore` は保存時に自動算出する
- 総合点の計算式は `各項目のスコア × 4 の合計`
- 内部キーは英語、画面表示は日本語にする
- 評価項目は以下で固定する
  - `story`: ストーリー
  - `character`: キャラクター
  - `visual`: ビジュアル
  - `music`: 楽曲
  - `rewatch`: リピートしたいか

## 実装時の優先順位

- まずは記録の作成、一覧表示、詳細確認、編集、削除を成立させる
- 次にランキング表示を実装する
- 検索、統計、共有、設定、同期は後回しにする

## コード変更時の期待

- UIレイヤーから直接DBを触らず、保存層を分離する
- 将来のクラウドSQLite移行を見越して、保存処理の差し替え余地を残す
- MVP段階では不必要な抽象化を避ける
- まず動くことと保守しやすいことを優先する

## テスト観点

- 記録が正しく保存されること
- アプリ再起動後も記録が永続化されること
- 総合点が期待通りに計算されること
- 編集や削除の結果がランキングへ反映されること
- 未保存変更がある状態で閉じると確認が出ること
